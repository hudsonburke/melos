[project]
name = "melos"
version = "0.1.0"
description = "Musculoskeletal modeling and cable-driven exoskeleton design framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "melos-core",
    "melos-sim",
]

[project.optional-dependencies]
mujoco = ["mujoco>=3.0"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pyright>=1.1",
]
all = [
    "melos-core",
    "melos-sim",
    "melos-skin",
    "melos-blender",
]

[tool.uv.workspace]
members = [
    "packages/melos-core",
    "packages/melos-blender",
    "packages/melos-sim",
    "packages/melos-skin",
]

[tool.uv.sources]
melos-core = { workspace = true }
melos-sim = { workspace = true }
melos-skin = { workspace = true }
melos-blender = { workspace = true }

[tool.pytest.ini_options]
testpaths = [
    "packages/melos-core/tests",
    "packages/melos-sim",
    "packages/melos-skin/tests",
]
